version: 2.1

jobs:
  build:
    docker:
      - image: 'cimg/python:3.9.6'
    steps:
      - checkout
      - sonarcloud/scan
orbs:
  sonarcloud: sonarsource/sonarcloud@1.0.1

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud